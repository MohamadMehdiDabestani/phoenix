name: 1-h
on:
  schedule:
    - cron: "0 */1 * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          curl --request GET \
          --url 'https://cryptomarketwatcher.herokuapp.com/crons'
